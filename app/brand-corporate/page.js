import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('brand-corporate');
export default function Page() { return <PageBody slug="brand-corporate" />; }

import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('thank-you');
export default function Page() { return <PageBody slug="thank-you" />; }

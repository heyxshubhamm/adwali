import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('email-marketing-automation');
export default function Page() { return <PageBody slug="email-marketing-automation" />; }

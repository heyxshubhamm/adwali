import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('social-community');
export default function Page() { return <PageBody slug="social-community" />; }

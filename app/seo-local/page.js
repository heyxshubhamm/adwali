import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('seo-local');
export default function Page() { return <PageBody slug="seo-local" />; }

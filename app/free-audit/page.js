import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('free-audit');
export default function Page() { return <PageBody slug="free-audit" />; }

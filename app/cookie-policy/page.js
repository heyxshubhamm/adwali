import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('cookie-policy');
export default function Page() { return <PageBody slug="cookie-policy" />; }

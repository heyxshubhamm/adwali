import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('social-strategy');
export default function Page() { return <PageBody slug="social-strategy" />; }

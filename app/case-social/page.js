import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('case-social');
export default function Page() { return <PageBody slug="case-social" />; }

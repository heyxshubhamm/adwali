import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('case-video');
export default function Page() { return <PageBody slug="case-video" />; }

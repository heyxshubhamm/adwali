import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('email-whatsapp');
export default function Page() { return <PageBody slug="email-whatsapp" />; }

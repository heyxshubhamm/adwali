import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('social-content');
export default function Page() { return <PageBody slug="social-content" />; }

import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('brand-orm');
export default function Page() { return <PageBody slug="brand-orm" />; }

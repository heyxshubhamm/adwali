import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('seo-onpage');
export default function Page() { return <PageBody slug="seo-onpage" />; }

import PageBody from '@/components/PageBody';
import { buildMetadata } from '@/lib/meta';
export const metadata = buildMetadata('video-motion');
export default function Page() { return <PageBody slug="video-motion" />; }
